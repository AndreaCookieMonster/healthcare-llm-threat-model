{
  "name": "healthcare-llm-threat-model",
  "private": true,
  "scripts": {
    "fmt": "prettier --write \"**/*.{md,mdx,yml,yaml}\"",
    "lint:md": "node scripts/lint-markdown.js",
    "lint:spell": "node scripts/spellcheck.js",
    "lint:yaml": "python3 scripts/lint-yaml.py",
    "lint:frontmatter": "node scripts/validate-frontmatter.js",
    "lint:dup": "python3 scripts/find-dup-content.py",
    "lint": "npm run lint:md && npm run lint:spell && npm run lint:yaml && npm run lint:frontmatter && npm run lint:dup",
    "build": "bundle exec jekyll build",
    "build:index": "node scripts/build-catalog-json.js"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "prettier": "^3.3.0",
    "yamllint": "file:."
  }
}
